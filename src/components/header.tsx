import React from "react";
export const Header = () => {
    return (
        <header className={"l-header"}>
            <div className={"l-header-inner"}>
                <a href={"/"} className={"c-logo"}>
                    digicons
                </a>
            </div>
        </header>
    );
};
