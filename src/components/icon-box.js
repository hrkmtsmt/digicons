import React from "react";
export const IconBox = (props) => {
    return (
        <div className={"l-grid-extra-small"}>
            <button className={"c-icon-box"}>{props.icon}</button>
        </div>
    );
};
